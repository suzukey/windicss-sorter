import type { Element, ElementWithWeight } from '@/interfaces'

export default class Sorter {
  public sort(windiElements: ElementWithWeight[]): Element[] {
    // TODO: Sort here

    const res = windiElements.slice()

    return res
  }
}
